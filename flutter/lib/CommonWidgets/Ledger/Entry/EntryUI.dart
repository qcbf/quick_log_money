import 'package:flutter/material.dart';

class EntryUI extends StatelessWidget {
  const EntryUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
